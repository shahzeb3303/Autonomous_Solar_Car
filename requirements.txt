# Autonomous Solar Vehicle - Python Dependencies
# Tested on Raspberry Pi 4 Model B (4GB RAM), Raspberry Pi OS 64-bit (Bullseye)
# Python 3.9.x

# Core ML/AI
tensorflow==2.10.1          # Full TF for training (use on dev machine)
# tflite-runtime==2.10.0    # Use this instead of full TF on RPi4 for deployment
numpy==1.23.5
scipy==1.9.3

# Computer Vision
opencv-python-headless==4.7.0.72   # Headless version for RPi (no GUI dependencies)
Pillow==9.4.0

# GPIO & Hardware (RPi-specific)
RPi.GPIO==0.7.1             # GPIO control - only install on RPi
# gpiozero==1.6.2           # Alternative GPIO library (optional)
picamera2==0.3.9            # Camera interface for RPi - only install on RPi
# picamera==1.13            # Legacy camera library (optional fallback)

# Serial Communication (GPS)
pyserial==3.5
pynmea2==1.18.0             # NMEA GPS sentence parsing

# Data Processing
pandas==1.5.3
scikit-learn==1.2.2

# Configuration & Utilities
python-dotenv==1.0.0
PyYAML==6.0

# Logging & Monitoring
structlog==23.1.0

# Testing
pytest==7.3.1
pytest-cov==4.1.0
pytest-mock==3.10.0
hypothesis==6.75.3          # Property-based testing

# Code Quality
flake8==6.0.0
black==23.3.0
isort==5.12.0
mypy==1.3.0

# Development Tools (not needed on RPi)
jupyter==1.0.0
matplotlib==3.7.1
seaborn==0.12.2
tensorboard==2.10.1

# Threading & Async
schedule==1.2.0

# Note: On Raspberry Pi 4, install these packages separately:
# - RPi.GPIO (comes pre-installed on Raspberry Pi OS)
# - picamera2 (install via: sudo apt install python3-picamera2)
# - For TFLite only deployment: pip install tflite-runtime (instead of full tensorflow)
